# Day 8 - Install Ansible

## Task
Install Ansible version `4.9.0` on the jump host using `pip3` only. Ensure the Ansible binary is globally available so all users on the system can run the `ansible` command.

---

## Server
- Jump Host

---

## Commands Used

### 1. Install Ansible using pip3
```bash
sudo pip3 install ansible==4.9.0

### 2. Verify installation
ansible --version


